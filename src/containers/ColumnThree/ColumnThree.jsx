const ColumnThree = (props) => {
    const { styles, children } = props

    return (
        // column three
        <div className={styles}>
            {children}
        </div>
    )
}
export default ColumnThree