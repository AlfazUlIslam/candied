const SubColumnTwo = (props) => {
    const { styles, children } = props

    return (
        // sub column two
        <div className={styles}>
            {children}
        </div>
    )
}
export default SubColumnTwo